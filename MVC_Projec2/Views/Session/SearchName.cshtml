@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@model List<Session>
@{
    ViewData["Title"] = "SearchSession";
}

<link rel="stylesheet" href="/css/DetailsStyle.css">
<link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet">
<link rel="stylesheet" href="/css/hallStyle.css">
<link rel="stylesheet" href="/css/bootstrap.min.css">


<div class="container-fluid">
    <div class="row text-center justify-content-center">
        <h1 class="mt-5 mb-5">Search Results for Session</h1>

        <!-- Search -->

        <div class="search-container">
            <form asp-controller="Session" asp-action="Search" method="get" class="search-box">
                <i class="fas fa-search"></i>
                <input type="text" name="searchValue" placeholder="Search by session name..." />
            </form>
        </div>

        <!-- Error -->
        @if (TempData["ErrorMessage"] != null)
        {
            <div class="alert-error">
                @TempData["ErrorMessage"]
            </div>
        }


        @foreach (Session session in Model)
        {
            <div class="col-lg-3 col-md-5 col-sm-12 mt-3 me-3">
                <div id="hallCard">
                    <img src="/images/@session.ImageUrl" alt="" class="mt-1">
                    <p class="m-2">@session.Name</p>
                    <hr class="m-2">
                    <strong class="m-2">View / request price</strong>
                    <a asp-controller="Session" asp-action="Details" asp-route-id="@session.Id"><i class="fa-solid fa-circle-arrow-right" style="font-size: 20px;"></i></a>
                    @*                     <a href="/Hall/HallDetailes/@hall.Id"><i class="fa-solid fa-circle-arrow-right" style="font-size: 20px;"></i></a>
 *@                </div>
            </div>
        }

    </div>

    <div class="row text-center justify-content-center">
        <form asp-controller="Atelier" asp-action="GetAll" method="post">
            <button id="btn" class="btn mt-5 w-25">Back to All Session</button>
        </form>
    </div>
</div>




<script src="/js/bootstrap.bundle.min.js"></script>



