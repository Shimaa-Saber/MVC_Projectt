@model MVC_Projec2.ViewModels.AddSessionViewModel

@{
    ViewData["Title"] = "Add Session";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow-lg rounded-4">
                <div class="card-header text-success bg-light fst-italic text-center fs-3">
                    Add New Session
                </div>

                <form asp-action="Create" enctype="multipart/form-data">
                    <div class="card-body">
                        <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>

                        <div class="mb-3">
                            <label asp-for="Type" class="form-label">Session Type</label>
                            <input asp-for="Type" class="form-control" />
                            <span asp-validation-for="Type" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Duration" class="form-label">Duration</label>
                            <input asp-for="Duration" class="form-control" />
                            <span asp-validation-for="Duration" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="ImageFiles" class="form-label">Upload Image</label>
                            <input asp-for="ImageFiles" class="form-control" type="file" />
                            <span asp-validation-for="ImageFiles" class="text-danger"></span>
                        </div>
                    </div>

                    <div class="card-footer">
                        <div class="d-flex justify-content-center gap-3">
                            <button type="submit" class="btn btn-success px-4">Add</button>
                            <a asp-action="GetAll" class="btn btn-outline-success px-4">Back to List</a>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @await Html.PartialAsync("_ValidationScriptsPartial")
}